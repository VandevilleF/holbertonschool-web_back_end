#!/usr/bin/env python3
"""A type-annotated function to_str"""


def to_str(n: float) -> str:
    """A type-annotated function to_str that takes a float n
    as arg and returns the string representation of the float."""
    return str(n)
