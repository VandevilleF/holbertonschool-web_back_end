#!/usr/bin/env python3
"""A type-annotated function concat"""


def concat(str1: str, str2: str) -> str:
    """A type-annotated func concat that takes a string str1
    and a string str2 as arg and returns a concatenated string"""
    return (str1 + str2)
